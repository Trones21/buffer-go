# Binaries
*.exe
*.out
bin/
# Compiled server binary (built via `go install`/`go build`)
/cmd/buffer-mcp/buffer-mcp
# Test artifacts
*.test
coverage.out
# Env / secrets — never commit a token
.env
.env.*
